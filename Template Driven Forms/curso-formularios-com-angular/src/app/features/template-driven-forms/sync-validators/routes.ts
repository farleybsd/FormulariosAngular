import { Routes } from "@angular/router";
import { SyncValidatorsComponent } from "./sync-validators.component";

export const routes:Routes = [
  {
    path: 'sync-validators',
    component: SyncValidatorsComponent
  }
]
